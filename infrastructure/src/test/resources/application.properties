# Test slice config: H2 with Hibernate-generated schema, bypassing the Postgres-specific
# Flyway migration (which uses syntax like `serial`/`timestamptz` that H2 doesn't share).
spring.flyway.enabled=false
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
